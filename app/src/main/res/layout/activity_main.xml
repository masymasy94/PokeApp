<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_main_gradient"
    tools:context="com.app.pokeapp.activities.MainActivity">

    <!-- Opzionale: Titolo o Logo dell'App in alto -->
    <TextView
        android:id="@+id/textViewAppTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:text="POKÉMON TRAINER APP"
        android:textColor="@color/text_primary_dark_theme"
        android:textSize="32sp"
        android:textStyle="bold"
        android:letterSpacing="0.05"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/textViewAppSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="App di supporto al gioco da tavolo"
        android:textColor="@color/text_secondary_dark_theme"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/textViewAppTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Contenitore per i bottoni, centrato verticalmente -->
    <LinearLayout
        android:id="@+id/buttons_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        app:layout_constraintTop_toBottomOf="@id/textViewAppSubtitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.4"
        app:layout_constraintWidth_default="wrap" >


    <!-- Per usare lo stile personalizzato su bottoni standard o AppCompatButton -->
    <!-- Se il tuo tema è un tema AppCompat (es. Theme.AppCompat.Light),
         Android Studio potrebbe usare automaticamente AppCompatButton per te.
         Altrimenti, puoi specificare android.support.v7.widget.AppCompatButton -->

    <Button
        android:id="@+id/pokedex_btn"
        style="@style/MainScreenButtonOldSyntax"
        android:text="Pokedex"/>

    <Button
        android:id="@+id/fight_btn"
        style="@style/MainScreenButtonOldSyntax"
        android:layout_marginTop="20dp"
        android:text="Sfida Allenatori"/>

    <Button
        android:id="@+id/big_fight_btn"
        style="@style/MainScreenButtonOldSyntax"
        android:layout_marginTop="20dp"
        android:text="Sfida Capopalestra"/>

    <Button
        android:id="@+id/random_fight_btn"
        style="@style/MainScreenButtonOldSyntax"
        android:layout_marginTop="20dp"
        android:text="Incontri Casuali"/>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>
